1.0.1:
  - "Initialize plugin"
1.0.2:
  - "Create tokens table"
  - create_tokens_table.php
1.0.3: Unserialize token before parsing to ensure compatibility with Cookie::get()
1.0.4: Lets CookieTokenAuth middleware return JSON if requested
1.0.5: Fixes redirect problem
1.0.6: Invalidates cookie auth token on logout
1.0.7: Handles male-formed token and inactive user exceptions
1.0.8: GraphQL support (requires Headstart plugin)
1.0.9: Prevent user from knowing whether entered email exists (thanks to @helmutkaufmann)
1.0.10: Bump Rainlab.User dependency
1.0.11: Random password and nocio.passwordless.user.created event on user creation (thanks to @massimomegistus)
1.0.12: nocio.passwordless.user.beforeLoginEmail event (thanks to @massimomegistus)
1.0.13: Add fallback base_url using Config::get('app.url') when currentPageUrl() fails (thanks to @snipiba)
1.0.14: Update dependency for RainLab.User to support v2 (thanks to @snipiba)
